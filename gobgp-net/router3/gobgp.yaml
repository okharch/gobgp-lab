global:
  config:
    as: 65003
    router-id: "172.20.2.254"

neighbors:
  - config:
      neighbor-address: "172.20.3.4" # router1's IP on net-core
      peer-as: 65001
  - config:
      neighbor-address: "172.20.3.2" # router2's IP on net-core
      peer-as: 65002

networks:
  - prefix: 172.20.2.0/24