global:
  config:
    as: 65001
    router-id: "172.20.0.254"

neighbors:
  - config:
      neighbor-address: "172.20.3.2" # router2's IP on net-core
      peer-as: 65002
  - config:
      neighbor-address: "172.20.3.3" # router3's IP on net-core
      peer-as: 65003

networks: # This should now be supported as a top-level key
  - prefix: 172.20.0.0/24